%div.content
  %table{class: "table table-striped table-bordered table-condensed"}
    %thead
      %tr
        %th 記事タイトル
        %th タグ
        %th ブクマ数
        %th 記事追加日
    - @data.each do|bookmark|
      %tr
        %td.title
          %a{href: bookmark.url}= bookmark.title
        %td.tag
          %a{href: "#{bookmark.tag}"}= bookmark.tag
        %td.count= bookmark.bookmark_count
        %td.create= bookmark.created_at.strftime("%Y-%m-%d")

